continue 的作用是用于跳过本次循环的后续代码，而继续执行下一次操作。

break与continue只能在循环中使用
区分break与continue：
break用于推出整个循环结构，continue用于结束本次循环进入下一次循环并不会推出循环结构。

pass是空语句的意思，保证语法结构完整，并无实际意义


序列是一个用于存储多个值的连续空间每个值都对用一个整数的编号，成为索引
其中索引分为正向索引和负向索引。

切片操作的语法结构：
序列[start:end:step] //step默认是1 如果没有start，则默认是从0开始就是开头，若没有end，则是默认到结束
另外，如果step=-1，表示反向输出。

序列的相加操作 类似于字符串的连接操作+ 把两个字符串连接在一起，相乘操作类似。

x in s / x not in s
len(s)  求s序列中元素的个数，即序列的长度
max(s)
min(s)  //最大最小值使用ASCLL码来比较
s.index(x) //用于检索s序列中第一个x出现的位置
s.count(x)//用于求s序列中x出现的个数

**************************************************

列表：指一系列按特定顺序排列的元素组成，元素可以是任意的数据类型，是可变序列。

在python中用[]来定义列表，元素与元素之间使用英文的逗号分隔开。

创建列表的两种方式：
(1)列表名=[element1,element2,element3.....]
(2)列表名=list(序列)

列表的删除：
del 列表名 